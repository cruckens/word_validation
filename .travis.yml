language: python
python:
  - "3.6"
# command to install dependencies
branches:
  only:
    - master
before_install:
  - chmod +x pytest.py
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - ./pytest.py `git diff --name-only HEAD^ | grep ".doc"`